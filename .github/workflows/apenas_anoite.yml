name: Rodar apenas à noite

on:
  schedule:
    - cron: "0 * * * *"  # roda a cada hora

jobs:
  noite:
    runs-on: ubuntu-latest
    steps:
      - name: Verificar horário
        run: |
          hora=$(date +%H)
          if [ $hora -ge 18 ] || [ $hora -lt 6 ]; then
            echo "Executando à noite 🌙"
          else
            echo "Fora do horário noturno, encerrando..."
            exit 0
          fi
