name: Rodar apenas aos sábados

on:
  schedule:
    - cron: "0 12 * * *"  # todos os dias ao meio-dia

jobs:
  sabado:
    runs-on: ubuntu-latest
    steps:
      - name: Verificar dia da semana
        run: |
          dia=$(date +%u)
          if [ $dia -eq 6 ]; then
            echo "Hoje é sábado! 🗓️"
          else
            echo "Não é sábado, encerrando..."
            exit 0
          fi
